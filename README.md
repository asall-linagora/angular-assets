# angular-vimond-assets


## Build & development

Run `grunt` for building and `grunt serve` for preview. Grunt will use the port 9000

## Testing

Running `grunt test` will run the unit tests with karma.
